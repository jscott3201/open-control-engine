# Workspace formatter rules. Stable-only — no nightly-required options.
# Mirrors the selene-db substrate so the two codebases read identically.
edition = "2024"
max_width = 100
use_field_init_shorthand = true
use_try_shorthand = true
newline_style = "Unix"
