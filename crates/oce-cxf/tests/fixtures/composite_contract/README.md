# Composite-subset conformance corpus

The fixture files behind the published composite-subset contract
(`docs/cxf-composite-subset.md`). An external CXF emitter can validate its output against exactly
these documents: everything under `accepted/` imports warning-free, everything under `warned/`
imports successfully with exactly its pinned warning vector, and everything under `rejected/`
fails with the pinned `(DiagCode, subject, message)` triple. The drivers live in
`crates/oce-cxf/tests/composite_contract_corpus.rs` (resolver layer) and
`crates/oce-api/tests/conformance.rs` (full `Engine::load_cxf` pipeline).

**Index scope:** this index covers the `*.jsonld` fixture files only. The accepted fixtures'
byte-exact goldens live in the shared golden tree
(`tests/fixtures/golden/composite_contract_*.modelgraph.txt`), not under this directory; re-bless
them with `OCE_BLESS=1 cargo test -p oce-cxf --test composite_contract_corpus`. The
`readme_index_lists_exactly_the_sorted_jsonld_corpus` test holds this index equal to the on-disk
listing — adding, removing, or renaming a fixture without updating this file fails the suite.

## Accepted

| Fixture | Rules | Demonstrates | Expected outcome |
| --- | --- | --- | --- |
| `accepted/forward_sibling_reference.jsonld` | 1, 3, 5, 6 | The mutual own scope: the root's first parameter references a later-declared sibling parameter AND a sibling constant (`kDerived = kBase + cShift`) — declaration array order carries no meaning for a composite's own scope, and a parameter may read a sibling constant. | Imports warning-free; golden `composite_contract_forward_sibling_reference.modelgraph.txt`. |
| `accepted/string_literal_sibling_name.jsonld` | 5 | Identifier-shaped text inside a String literal is data rather than a sibling reference: `a = "b"` beside own declaration `b = a` is an acyclic chain, not a mutual cycle. | Imports warning-free under every declaration order; golden `composite_contract_string_literal_sibling_name.modelgraph.txt`. |
| `accepted/leaf_array_parameter_conditional_member.jsonld` | 1, 5 | A leaf carrying a legal array parameter (`holdSet[2]`, expanded per-element) plus a false-guarded conditional input. The specialization pass grounds the leaf's chain for the guard, but its generic grounding machinery is non-emitting — the array binding's scalar-only grounding failure there no longer refuses the document. | Imports warning-free (conditional input pruned); golden `composite_contract_leaf_array_parameter_conditional_member.modelgraph.txt`. |
| `accepted/leaf_identity_parameter_modification.jsonld` | 1, 5 | The leaf identity-modification idiom: a leaf parameter `samplePeriod = "samplePeriod"` reading the same-named enclosing composite parameter, beside a false-guarded conditional input. The self-reference is a member modification, not an own-chain cycle — the tagged rules apply only to composite chains at the specialization pass — and the member value grounds enclosing-first. | Imports warning-free (`con.samplePeriod = 120.0`, conditional input pruned); golden `composite_contract_leaf_identity_parameter_modification.modelgraph.txt`. |
| `accepted/minimal_nested.jsonld` | 1, 3, 5, 6 | One unregistered composite under the root; sibling parameter reference (`kTop = kBase + 0.25`), `hasConstant` grounding, parent-scope inheritance into the leaf gain; boundary input rewired to the leaf and non-top boundary output followed through. | Imports warning-free; golden `composite_contract_minimal_nested.modelgraph.txt`. |
| `accepted/two_level_nesting.jsonld` | 1, 3, 5, 6 | Two composite levels (`root → outer → inner → leaf`); the parameter scope chains across both levels (`kRoot → kOuter → kInner = kOuter + 1.0 → gain.k = 4.0`); boundary connectors elided at every level. | Imports warning-free; golden `composite_contract_two_level_nesting.modelgraph.txt`. |
| `accepted/registered_leaf_carveout.jsonld` | 1 | The rule 1 carve-out: a registered `Constant` leaf carrying `containsBlock` (a protected implementation child) under a valid unregistered top composite. The leaf imports as a normal block; the protected child is elided. | Imports warning-free; golden `composite_contract_registered_leaf_carveout.modelgraph.txt`. |
| `accepted/agreeing_parameter_values.jsonld` | 7 | A both-carrying `UnitDelay` whose `hasParameter` route and `hasInstance` member both value `y_start` at the same 7.0 — the silent control proving the conflicting-declaration Error is caused by value divergence, not by the two-route shape. | Imports warning-free; golden `composite_contract_agreeing_parameter_values.modelgraph.txt`. |
| `accepted/carveout_member_array_unreferenced.jsonld` | 7 | The carve-out array pair's control: the `isArray`-marked node exists but is referenced by neither `hasInstance` nor `hasInput`, proving the array scan is reference-based, not position-based. | Imports warning-free; golden `composite_contract_carveout_member_array_unreferenced.modelgraph.txt`. |
| `accepted/inactive_parameter_declaration_grounding.jsonld` | 7 | The ordinary-declaration twin of `inactive_parameter_member_grounding`: the active `Constant` names `k` through `hasParameter` while the pruned sibling marks the same node inactive. Leaf parameter grounding does not consult activity on either route. | Imports warning-free; its golden is byte-identical to the member-route fixture's. |
| `accepted/inactive_parameter_member_grounding.jsonld` | 7 | A false-guarded sibling's authored `hasInput` marks an active derivation-shaped `Constant` instance's `k` member inactive. Activity filters derived ports, not parameter members, so `k = 7.0` still grounds through the member appendix. | Imports warning-free; golden `composite_contract_inactive_parameter_member_grounding.modelgraph.txt` pins the parameter value. |
| `accepted/inert_bothcarrying_array_member.jsonld` | 7 | A both-carrying child's discarded `hasInstance` list naming an `isArray`-marked node — the list is not an array-scan source outside the derivation shape. | Imports warning-free; golden `composite_contract_inert_bothcarrying_array_member.modelgraph.txt`. |
| `accepted/inert_bothcarrying_depth2_member.jsonld` | 7 | A both-carrying child's discarded list naming a depth-2 member whose local name the class does not declare — the class-declared-name filter keeps junk members inert. | Imports warning-free; golden `composite_contract_inert_bothcarrying_depth2_member.modelgraph.txt`. |
| `accepted/inert_bothcarrying_instance_member.jsonld` | 7 | A both-carrying child's discarded list naming the child instance itself — never domain-checked outside the derivation domain. | Imports warning-free; golden `composite_contract_inert_bothcarrying_instance_member.modelgraph.txt`. |
| `accepted/inert_nested_composite_array_member.jsonld` | 7 | A runtime composite's `hasInstance` naming an `isArray`-marked node — a composite's list contributes nothing to the array scan (refusing control: the nested-composite hasInput rejection fixture). | Imports warning-free; golden `composite_contract_inert_nested_composite_array_member.modelgraph.txt`. |
| `accepted/inert_nested_composite_depth2_member.jsonld` | 7 | A runtime composite's `hasInstance` naming a depth-2 member — inert on every path. | Imports warning-free; golden `composite_contract_inert_nested_composite_depth2_member.modelgraph.txt`. |
| `accepted/inert_nested_composite_instance_member.jsonld` | 7 | A runtime composite's `hasInstance` naming its own leaf instance — inert on every path. | Imports warning-free; golden `composite_contract_inert_nested_composite_instance_member.modelgraph.txt`. |
| `accepted/inert_orphan_array_member.jsonld` | 7 | An orphan node — neither root nor `containsBlock` referent — whose list names an `isArray`-marked connector; an orphan's list is never scanned (refusing control: the orphan hasInput rejection fixture). | Imports warning-free; golden `composite_contract_inert_orphan_array_member.modelgraph.txt`. |
| `accepted/inert_pruned_bothcarrying_list.jsonld` | 7 | A pruned conditional child that declares its own ports and whose discarded list names an active sibling's input — the list is outside the traversal's shape domain and strips nothing. | Imports warning-free; golden `composite_contract_inert_pruned_bothcarrying_list.modelgraph.txt`. |
| `accepted/inert_pruned_shape_self_member.jsonld` | 7 | A pruned derivation-shaped child whose list names its own member while an active sibling declares that node on its `hasInput` — reference exclusivity keeps the member active; an owner-prefix test would refuse this document. | Imports warning-free; golden `composite_contract_inert_pruned_shape_self_member.modelgraph.txt`. |
| `accepted/inert_root_conditional_member.jsonld` | 7 | A root's `hasInstance` naming a node marked `isConditionalComponent` with no guard expression — a root's list seeds no conditional candidate, so the missing guard never diagnoses. | Imports warning-free; golden `composite_contract_inert_root_conditional_member.modelgraph.txt`. |
| `accepted/inert_root_dangling_member.jsonld` | 7 | A root's `hasInstance` naming an IRI with no node — a root-carried list is silent by ruling, not by omission. | Imports warning-free; golden `composite_contract_inert_root_dangling_member.modelgraph.txt`. |
| `accepted/inert_root_empty_list.jsonld` | 7 | A root carrying an empty `hasInstance` list. | Imports warning-free; golden `composite_contract_inert_root_empty_list.modelgraph.txt`. |
| `accepted/member_array_permutation.jsonld` | 7 | The mixed fixture with every `hasInstance` array reordered (same `@id`s) — array order is load-bearing for nothing: `ConnectorId`s, `decl_order` and `param` rows stay byte-identical. | Imports warning-free; golden `composite_contract_member_array_permutation.modelgraph.txt` (byte-identical to the mixed fixture's). |
| `accepted/mixed_member_interface.jsonld` | 7 | The `hasInstance` interface derivation end-to-end: node-bearing and node-less members, one internally driven and one externally driven derived input, so the member-IRI identity and the boundary re-keyed identity are pinned against each other. | Imports warning-free; golden `composite_contract_mixed_member_interface.modelgraph.txt`. |
| `accepted/padded_output_tie.jsonld` | 7 | A `CDL.Logical.And` with node-bearing `u1`, node-less listed `u2` and padded `y` — the synthesized-connector tie whose per-list key would invert the ruled inputs-then-outputs concatenation order, with two node-bearing connectors ahead of the synthesized run so `decl_order` mutations are observable. | Imports warning-free; golden `composite_contract_padded_output_tie.modelgraph.txt`. |
| `accepted/synthesized_order_across_owners.jsonld` | 7 | Two derivation-shaped owners whose synthesized keys discriminate R19-5's key COMPOSITION, not merely its direction: the earlier `@graph` owner `gain` mints its OUTPUT (concat index 1) while the later owner `del` mints its INPUT (index 0), so `(owner_pos, concat_index)` orders them `gain.y` then `del.u` while any index-first or owner-blind key swaps them. Every shipped fixture before this one tied at index 0, leaving three of four ordering mutations green. | Imports warning-free; golden `composite_contract_synthesized_order_across_owners.modelgraph.txt`. |
| `accepted/two_level_nesting_inner_list.jsonld` | 7 | The shipped `two_level_nesting.jsonld` with one `hasInstance` key added to its unregistered inner composite — the comparison domain's population and resolved-class tests each keep it inert on their own. | Imports warning-free; golden `composite_contract_two_level_nesting_inner_list.modelgraph.txt`. |
| `accepted/unclassifiable_member_control.jsonld` | 7 | The six-member `CDL.Reals.Sources.Sin` control twin: five node-bearing parameters plus node-less output `y`. Its rejected twin changes only that final member name to `zzz`, preserving count and node-bearing shape. | Imports warning-free; golden `composite_contract_unclassifiable_member_control.modelgraph.txt`. |

## Warned

| Fixture | Rule | Demonstrates | Expected outcome |
| --- | --- | --- | --- |
| `warned/undriven_boundary_output.jsonld` | — | A root `hasOutput` declaring a node (`#M.y`) that exists but that no internal connector or boundary input drives. Untagged boundary-interface machinery, not a composite-shape rule. | Loads successfully with exactly one `undriven-boundary-output` **warning** (subject `#M.y`); the declared output enters no point surface. |
| `warned/analog_coerced_member.jsonld` | 7 | A derived member connector node typed `S231:AnalogInput` on a `Real`-signature port — the `Analog*` bridge resolves to `Real` with an advisory. | Loads successfully with exactly one `analog-coerced-to-real` **warning** (subject the member node). |
| `warned/conflicting_interface_name.jsonld` | 7 | A both-carrying `UnitDelay` whose `hasInstance` carries the class-declared parameter name `y_start` on no route of the node's own — the one-directional (list minus own) comparison reports it. | Loads successfully with exactly one `conflicting-interface-declaration` **warning** (subject the instance node). |

## Rejected

| Fixture | Rule | Demonstrates | Expected outcome |
| --- | --- | --- | --- |
| `rejected/multi_root.jsonld` | 2 | Two unreferenced composites (`#M`, `#M2`). | `composite/root-count` (`malformed-document`); candidates enumerated in `@graph` order, first candidate is the subject. |
| `rejected/pure_cycle.jsonld` | 2 | A pure `containsBlock` cycle `A→B→C→A` with no root at all. | `composite/root-count` (`malformed-document`) with **no subject** — zero candidate roots; never reported as a cycle. |
| `rejected/reachable_cycle.jsonld` | 4 | A valid root reaching the cycle `A→B→C→A`. | `composite/contains-cycle` (`malformed-document`) naming all participants in path order, closing with the re-entered id `#A`, which is the subject. |
| `rejected/self_loop.jsonld` | 4 | A composite containing itself (`A→A`). | `composite/contains-cycle` (`malformed-document`); the degenerate two-entry participant list `#A -> #A`. |
| `rejected/diamond_cycle.jsonld` | 4 | One structural cycle reachable via two paths (root contains `A` and `B`; `A→C`, `B→C`, `C→A`). | **Two** `composite/contains-cycle` (`malformed-document`) diagnostics — one per re-entry (`#A -> #C -> #A` subject `#A`, then `#C -> #A -> #C` subject `#C`); consumers must not assume one diagnostic per structural cycle. |
| `rejected/declaration_cycle.jsonld` | 5 | A reference cycle between two of the root's own parameters (`a = "b + 1.0"`, `b = "a + 1.0"`). | `composite/declaration-cycle` (`malformed-document`); ONE diagnostic per distinct cycle **per chain evaluation** — a composite evaluated once per `containsBlock` path can surface the same cycle once per visit, like `contains-cycle`. Subject is the participant earliest in chained declaration order (`#M.a`); the message's arrow list is the participant ring in chained order closing on the first, not the discovered edge path. |
| `rejected/self_reference.jsonld` | 5 | A self-referencing inner-composite parameter (`x = "x * 2.0"`) under a same-named enclosing binding — a length-1 cycle, never an enclosing read. | `composite/declaration-cycle` (`malformed-document`); the degenerate two-entry participant list `#M.sub.x -> #M.sub.x`. |
| `rejected/duplicate_declaration.jsonld` | 5 | One local name (`k`) bound twice in one composite's own chain — once under `hasParameter`, once under `hasConstant`. | `composite/duplicate-declaration` (`malformed-document`); one diagnostic per occurrence beyond the first in chained order, subject is that later occurrence (`#M.settings.k`), message names it and the first occurrence's `@id`. |
| `rejected/banned_key_bare.jsonld` | 7 | The bare banned key spelling (`redeclare`). | `composite/banned-modelica-key` (`non-subset-construct`); subject is the owning node; the message names the key as authored. |
| `rejected/banned_key_prefixed.jsonld` | 7 | The prefixed banned key spelling (`S231:extendsFrom`). | `composite/banned-modelica-key` (`non-subset-construct`); subject is the owning node; the message names the key as authored. |
| `rejected/banned_key_absolute_iri.jsonld` | 7 | The absolute-IRI banned key spelling (`http://data.ashrae.org/S231P#moSource`). | `composite/banned-modelica-key` (`non-subset-construct`); subject is the owning node; the message names the key as authored. |
| `rejected/array_parameter.jsonld` | 5 | An `isArray` parameter on the top composite. | `composite/array-parameter` (`non-subset-construct`); subject is the parameter node `#M.p`. |
| `rejected/array_connector.jsonld` | 7 | An active connector referenced by a block instance, carrying both array markers (`isArray: true` and `sizeOfDimensions`); either marker alone also rejects. | `composite/array-connector` (`non-subset-construct`); subject is the connector node `#M.c2.u`. |
| `rejected/array_instance.jsonld` | 7 | An active `isArray` block instance in `containsBlock`. | `composite/array-instance` (`non-subset-construct`); subject is the instance node `#M.c2`. |
| `rejected/replaceable.jsonld` | 7 | `S231:isReplaceable: true` on a child component. | `composite/replaceable` (`unresolved-polymorphism`); subject is the replaceable node `#M.c2`. |
| `rejected/shadowed_output_child_connector.jsonld` | — | The root's `hasOutput` referencing a child instance's own port node (`#M.c1.y`), so the declared identity is an existing connector path. Untagged boundary-interface machinery. | `boundary-output-shadows-connector`; subject is the shadowing declared IRI `#M.c1.y`. |
| `rejected/shadowed_output_input_output.jsonld` | — | One IRI (`#M.io`) listed in both the root's `hasInput` and `hasOutput`, so the "declared output" is an input path. Untagged boundary-interface machinery. | `boundary-output-shadows-connector`; subject is the dual-listed IRI `#M.io`. |
| `rejected/multi_driven_boundary_output.jsonld` | — | Two child outputs both wired to one declared boundary output (`#M.y`). Untagged boundary-interface machinery. | `single-assignment`; subject is the multiply driven declared IRI `#M.y`. |
| `rejected/vector_port_instance.jsonld` | 7 | A `hasInstance`-derived instance of `CDL.Routing.RealScalarReplicator`, whose port count tracks a parameter — slice 1 derives scalar interfaces only. | `composite/vector-port-instance` (`non-subset-construct`); subject the instance node, one per instance, replacing the arity mismatch. |
| `rejected/unsupported_instance_member.jsonld` | 7 | The six-member Sin control with only the final node-less member renamed from declared output `y` to unclassifiable `zzz`. | `composite/unsupported-instance-member` (`non-subset-construct`); subject the member IRI; the instance derives no interface. |
| `rejected/member_outside_owner.jsonld` | 7 | A member (`#M.other.q`) outside its owner's namespace — `local_name` must never attach a foreign node. | `composite/unsupported-instance-member` (`non-subset-construct`); subject the member IRI. |
| `rejected/member_is_instance.jsonld` | 7 | A direct member that itself carries `hasInstance` — the property must not become a second containment edge. | `composite/unsupported-instance-member` (`non-subset-construct`); subject the member IRI. |
| `rejected/minted_identity_shadows_node.jsonld` | 7 | A padded output whose synthesized identity (`#M.del.y`) is already an `@graph` node; the skipped instance also carries a node-bearing member and the document later connectors, so keeping withdrawn members numbered is observable. | `composite/colliding-member-identity` (`non-subset-construct`); subject the colliding IRI; exactly one diagnostic. |
| `rejected/minted_identity_twice.jsonld` | 7 | One node-less port member listed twice in one array — the identity would be minted twice for one owner; neither pre-existing duplicate guard can see it. | `composite/colliding-member-identity` (`non-subset-construct`); subject the repeated member IRI. |
| `rejected/param_member_duplicates_hasparameter.jsonld` | 7 | A classified parameter member duplicating a name the instance's own `hasParameter` declares. | `composite/colliding-member-identity` (`non-subset-construct`); subject the member IRI. |
| `rejected/param_member_declared_twice.jsonld` | 7 | One parameter member IRI listed twice in one array. | `composite/colliding-member-identity` (`non-subset-construct`); subject the repeated member IRI. |
| `rejected/array_marked_member.jsonld` | 7 | An `isArray`-marked member of a derivation-domain instance — the array scan reads active derivation-shaped lists reference-based and class-independent. | `composite/array-connector` (`non-subset-construct`); subject the member node. |
| `rejected/array_marked_nested_composite_input.jsonld` | 7 | The array marker on a nested composite's `hasInput` — the EXISTING source, pinned so the new source's narrower shape does not move it. | `composite/array-connector` (`non-subset-construct`); subject the connector node. |
| `rejected/array_marked_orphan_input.jsonld` | 7 | The array marker on an orphan node's `hasInput` — the existing source is reachability-blind and stays so. | `composite/array-connector` (`non-subset-construct`); subject the connector node. |
| `rejected/carveout_member_array_hasinstance.jsonld` | 7 | A registered leaf's carve-out child carrying an `isArray`-marked node on its `hasInstance` — an active `containsBlock` referent lowering never reaches is still scanned. | `composite/array-connector` (`non-subset-construct`); vector byte-equal to the `hasInput` twin's. |
| `rejected/carveout_member_array_hasinput.jsonld` | 7 | The same carve-out shape with the marker on `hasInput` — the dialect-agreement twin. | `composite/array-connector` (`non-subset-construct`); vector byte-equal to the `hasInstance` twin's. |
| `rejected/nodeless_parameter_member.jsonld` | 7 | A classified parameter member whose IRI resolves to no node — refused by the derivation itself, never routed through the parameter loop's `unresolved-reference` arm. | `grounding-failed`; subject the member IRI; message `parameter has no value (Ground mode)`. |
| `rejected/member_isofdatatype_unresolved.jsonld` | 7 | A member node whose `isOfDataType` names no known datatype — the declaration keeps its existing diagnostic and the connector takes the signature kind, never the `Real` placeholder. | `unresolved-reference`; subject the datatype IRI. |
| `rejected/member_string_type.jsonld` | 7 | A member node with a `String` `@type`. | `malformed-document`; subject the member node; message `String connector not permitted (§7.8)`. |
| `rejected/member_unrecognized_type.jsonld` | 7 | A member node with an unrecognized `@type` term. | `malformed-document`; subject the member node; message `connector lacks a recognized data type`. |
| `rejected/missing_input_member.jsonld` | 7 | A `CDL.Logical.And` listing only `u1` and `y` — a missing declared input is never padded; the short vector takes the existing arity refusal. | `malformed-document`; subject the instance node; the existing arity message, unchanged. |
| `rejected/inactive_member_hasinstance.jsonld` | 7 | A pruned sibling's authored `hasInput` marks the active instance's listed member inactive — the member derives no connector and enters neither `ConnectorId` block. | `malformed-document` (existing arity message); vector byte-equal to the `hasInput` twin's. |
| `rejected/inactive_member_hasinput.jsonld` | 7 | The same shape spelled on `hasInput` — the dialect-agreement twin. | `malformed-document` (existing arity message); vector byte-equal to the `hasInstance` twin's. |
| `rejected/partial_port_declaration.jsonld` | 7 | A node declaring ONLY `hasOutput` beside a complete member list — the fallback is all-or-nothing, so the node keeps its declared (short) interface and the comparison names the input carried on the list alone. | The existing arity `malformed-document` and `unresolved-reference`, plus exactly one `conflicting-interface-declaration` **warning** naming `u`. |
| `rejected/conflicting_parameter_values.jsonld` | 7 | A both-carrying node whose `hasParameter` route values `y_start` at 3.0 while its `hasInstance` member carries 7.0 — two values for one name state a contradiction. | `conflicting-interface-declaration` **error**; subject the instance node. |
