//! Per-block tests for the native catalog: feedthrough classification, algebraic step semantics,
//! the loop-breakers' emit-then-latch cycle, and registry resolution.

mod common;
mod contract;
mod primitive_contract;
mod routing_typed_contract;
mod sources_and_logical;
